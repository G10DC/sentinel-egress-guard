# Sentinel — Roadmap

## Phase 1 (Current)
- Basic fetch hijacking with allowlist and regex payload checking.
- Native Node.js test runner integration.

## Phase 2 (Planned)
- TCP/UDP socket interception middleware.
- Configurable rules via YAML configuration files.

## Phase 3 (Future)
- Autonomous prompt injection classifier at network boundary.
